# Implementation note

The first implementation intentionally keeps the grammar small and typed. Expansion should add primitives only when they represent distinct observable filmmaking behavior rather than synonyms or provider-specific tricks.
