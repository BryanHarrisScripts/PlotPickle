name: STORY Phase 2 Story Piece Proof

on:
  pull_request:
    paths:
      - 'modules/story-the-unwritten/**'
      - 'tests/issue-1675-story-*.test.mjs'
      - '.github/workflows/story-phase2-story-piece-proof.yml'

permissions:
  contents: read

jobs:
  story-piece-proof:
    name: Exact-head STORY Story Piece persistence proof
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
      - name: Run all Phase 2 STORY persistence proofs
        run: node --test tests/issue-1675-story-persistence.test.mjs tests/issue-1675-story-sparse-characters.test.mjs tests/issue-1675-story-history-persistence.test.mjs tests/issue-1675-story-canon-admission.test.mjs tests/issue-1675-story-piece-persistence.test.mjs
