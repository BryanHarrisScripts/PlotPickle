# Issue 1868 acceptance

The change is complete when:

1. PlotPickle owns an original provider-independent cinematography grammar contract and vocabulary.
2. Freeform story intent can deterministically select a bounded set of compatible primitives.
3. Still/video applicability prevents motion-only instructions from leaking into still-image direction.
4. Conflicting primitives are suppressed rather than emitted together.
5. Sequence Director includes grammar output in surface briefs and per-render-clip prompts.
6. The compiler contains no provider/model/runtime/API-key authority.
7. Existing 24/96 addressing, 75-second Mini-Block timing, 3-second render-slot mapping, continuity and approval behavior remain unchanged.
8. Focused regression tests and normal PR/Product gates are green on the exact head.
