# UI conformance component families

Initial automated component families are surfaces, panels, controls, pills, headings, body text and status indicators. Native semantics and existing stable data/ARIA contracts should be used before adding test-only attributes. Any added semantic attribute describes a durable component role rather than a particular screenshot position.