# AI provider Settings

This directory owns the exact in-place cloud-provider configuration surface used by Settings → AI Routing. Provider credentials continue to be stored and verified by `/api/local-ai/connection`; this UI never persists or renders secret values itself.
