import assert from "node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const read=p=>readFile(new URL(`../${p}`,import.meta.url),"utf8");
test("#2426 supersedes #2424 and restores #2399 Outline",async()=>{const [h,s,m]=await Promise.all([read("app/skin-v1/dashboard-bbs-review-host.tsx"),read("app/skin-v1/matrix-story-map-surface.tsx"),read("modules/build/ui/progressive-story-map.tsx")]);const o=h.slice(h.indexOf("if (outlineOpen)"),h.indexOf("if (buildOpen)"));assert.ok(o.indexOf("<StoryActRail")<o.indexOf("<MatrixStoryMapSurface"));assert.match(s,/deriveOutlineReadiness\(project\)/);assert.match(s,/<ActWrittenStoryBoard/);assert.doesNotMatch(s,/data-outline-hierarchy/);assert.match(m,/surfaceLabel = "Outline"/);});
