# No duplicate UI authority

No future UI conformance implementation may introduce an independent token file that can disagree with `app/design-tokens.css` and the active Skin definition. Machine-readable expectations must resolve from, mirror under tested synchronization with, or be generated from the canonical repository contracts.