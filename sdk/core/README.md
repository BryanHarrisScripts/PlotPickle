# @plotpickle/sdk

Preview public client and Core Services SDK for connecting to a PlotPickle host.

```ts
import { connectPlotPickle } from "@plotpickle/sdk";
```

Depends only on `@plotpickle/types`; application routes and stores are not public dependencies.
