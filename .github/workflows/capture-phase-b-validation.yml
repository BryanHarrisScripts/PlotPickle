name: Capture Phase B validation

on:
  pull_request:
    branches:
      - main
    types:
      - ready_for_review

permissions:
  contents: write

jobs:
  capture:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase-b-diagnostic-craft-014
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Capture build and tests
        run: |
          set +e
          npm run build > docs/phase-b-build.log 2>&1
          echo $? > docs/phase-b-build.exit
          npm test > docs/phase-b-test.log 2>&1
          echo $? > docs/phase-b-test.exit
          exit 0
      - name: Commit validation diagnostics
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/phase-b-build.log docs/phase-b-build.exit docs/phase-b-test.log docs/phase-b-test.exit
          git commit -m "Capture Phase B validation diagnostics"
          git push origin HEAD:agent/phase-b-diagnostic-craft-014
