import assert from "node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const read=p=>readFile(new URL(`../${p}`,import.meta.url),"utf8");
test("#2426 restores Outline sizing while Storyboard keeps #2408",async()=>{const c=await read("app/skin-v1-surface-orchestrator.css");assert.match(c,/story-map"\][^]*?width:calc\(100% - 40px\)/);assert.match(c,/storyboard"\][^]*?width:calc\(100vw - 40px\)/);assert.match(c,/min-height:calc\(100dvh - 40px\)!important/);});
