# #2300 — Full QA Advisory Convergence

## Purpose

Converge the remaining findings from the first successful WebMCP FULL QA run by repairing shared root causes rather than patching warning instances.

Evidence baseline: `main@fb362cd76d370d080c5884e2a0016e9ae10b454e`, run `webmcp-full-qa-2026-09-20T17-59-07-889Z-82a140b1`.

## Root fixes

- Reset the Interaction probe to neutral BODY focus before measuring first-Tab entry so auto-focused directory controls are not mistaken for failed entry.
- Enrich Runtime clipped-control evidence with a bounded accessible identity and element/root geometry before any repeated-screen repair is authorized.
- Let the Write editor-inspector governed root grow to content height.
- Keep Storyboard and Previs child frames inside the already-governed root rather than recomputing width from `100vw`.
- Remove Node's nested `100vh` minimum.
- Move Issue Log and General review-control padding to Skin V1 spacing tokens.
- Keep one Profile content heading visually dominant over supporting section headings.

## Boundaries

Do not suppress advisories, add per-screen tabindex hacks, flatten valid subordinate grids, alter the five-stage pre-production order, change Dashboard authority, or create another QA framework.

## Verification

Focused deterministic contracts cover every root change. Exact-head Architecture Verification remains merge authority. Post-merge Human FULL QA is the final rendered proof for advisory-count convergence and the newly enriched clipped-control diagnosis.
