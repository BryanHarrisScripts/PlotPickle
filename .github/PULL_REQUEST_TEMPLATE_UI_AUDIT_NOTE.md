<!-- Internal implementation note for the UI/UX required merge gate. -->

The required check name is `Audit UI/UX against Design Rules`. It must remain synchronized across `.github/workflows/ui-ux-code-audit.yml`, `config/public-repository.settings.json`, and the public repository contract tests.
