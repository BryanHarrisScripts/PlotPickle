# UI conformance future expansion

Additional Skin surfaces should join the same semantic audit by adding stable navigation/registry metadata and reusable component-role coverage. Do not add one-off visual checkers per screen.