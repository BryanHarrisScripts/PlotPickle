# 2470 Phase 1: observation foundation

Option [3] requires a data-minimized development observation bus before a candidate may be presented to the Human. Reuse the closed-loop UAT runner and its existing fingerprints. Store an explicit expectation source and exact build identity for every candidate. Findings without contract-backed expectations remain unproven; no automatic claim of a product bug is permitted. N suppresses the unchanged candidate for its head, and Y records confirmation only. UAT evidence is development data outside the active PPF story. The closed-loop runner's existing `--github-report` and `--repair` flags are forbidden in its Option-3 observation mode.

This phase provides the pure finding/referee contract and bounded observation output. Later phases wire the actual journey observer, rendered checkpoint, safe navigation, Y-confirmed DSDD interpretation and publication, and end-to-end proof. This phase does not close #2470.
