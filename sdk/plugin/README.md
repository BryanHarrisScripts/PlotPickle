# @plotpickle/plugin-sdk

Preview plugin lifecycle, typed events, registrations and permission-aware Core Services.

```ts
import type { PluginModule } from "@plotpickle/plugin-sdk";
```

Testing helpers remain available through `@plotpickle/plugin-sdk/testing`; new projects should prefer `@plotpickle/testing`.
