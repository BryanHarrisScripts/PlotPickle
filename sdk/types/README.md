# @plotpickle/types

Preview public TypeScript contracts for PlotPickle projects, plugins and Core Services.

```ts
import type { PlotPickleProject, PluginManifest } from "@plotpickle/types";
```

Status: **preview**. Breaking changes require release notes and a coordinated SDK version bump.
