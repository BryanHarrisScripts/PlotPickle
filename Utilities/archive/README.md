# Archived Utilities

Files in this folder are retained only as historical references. They are intentionally renamed to `.arc` so Windows does not present them as supported executable launchers.

Do not run or restore these files as normal PlotPickle utilities. Use the supported launchers in the parent `Utilities` folder instead.
